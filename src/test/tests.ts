import { expect } from "chai";

describe("Test", () => {
    describe("1", () => {
        it("success", () => {
            expect(1).to.be.equal(1);
        });

        it("not success", () => {
            expect(1).to.be.equal(1);
        });
    });
});